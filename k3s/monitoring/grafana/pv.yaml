apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-dashboards-pv
spec:
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteMany
  nfs:
    server: 172.20.50.160
    path: /srv/grafana-dashboards
  persistentVolumeReclaimPolicy: Retain
